<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--   your app branding color for the app bar -->
        <item name="colorPrimary">@color/primary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="colorAccent">@color/accent</item>

        <item name="android:windowBackground">@color/green_200</item>
    </style>

    <style name="AppTheme.TranslucentStatusBar">
    </style>

    <style name="AppTheme.OpaqueStatusBar">
    </style>

    <style name="DrawerHandler">

    </style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">#1f000000</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="DrawerToggle">
        <item name="android:background">@drawable/btn_drawer_toggle</item>
        <item name="android:src">@drawable/ic_menu_white_24dp</item>
    </style>

    <style name="DrawerButton">
        <item name="android:background">@drawable/btn_drawer_background_selector</item>
        <item name="android:gravity">start|center_vertical</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:drawablePadding">32dp</item>
        <item name="fontPath">fonts/Roboto-Medium.ttf</item>
        <item name="android:textSize">14sp</item>
        <item name="android:capitalize">characters</item>
        <item name="android:textColor">@color/black_87</item>
        <item name="android:textIsSelectable">false</item>
    </style>
    
    <style name="DrawerButton.Activable">
        <item name="android:textColor">@color/btn_drawer_activable_textcolor_selector</item>
    </style>

    <style name="CustomShowcaseTheme" parent="ShowcaseView.Light">
        <item name="sv_backgroundColor">@color/black_85</item>
        <item name="sv_showcaseColor">#BF00a621</item>
        <item name="sv_tintButtonColor">true</item>
        <item name="sv_buttonText">OK</item>
        <item name="sv_titleTextAppearance">@style/TitleCustomShowcaseTheme</item>
        <item name="sv_detailTextAppearance">@style/DetailCustomShowcaseTheme</item>
    </style>

    <style name="TitleCustomShowcaseTheme" parent="TextAppearance.ShowcaseView.Title.Light">
        <item name="android:textColor">@color/white</item>
        <item name="android:shadowRadius">0</item>
    </style>

    <style name="DetailCustomShowcaseTheme" parent="TextAppearance.ShowcaseView.Detail.Light">
        <item name="android:textColor">@color/white</item>
        <item name="android:shadowRadius">0</item>
    </style>

</resources>
